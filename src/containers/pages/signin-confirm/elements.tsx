import styled from 'styled-components';

import { AppBox } from '../../app/elements';

export const SignInBox = styled(AppBox)`
  width: 100%;
`;

export const SignInInner = styled.div`
  width: 375px;
  display: block;
  margin: 0 auto;
`;
