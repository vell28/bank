import styled from 'styled-components';

export const PreviewContainer = styled.div`
    display: flex:
    align-items: center;
`;

export const SentToLabel = styled.label`
  color: ${(props) => props.theme.colors.black};
`;

export const AddressField = styled.div`
  color: ${(props) => props.theme.colors.black};
`;
